server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhengjxnupt/springcloud-study
          searchPaths: spring-cloud/helloworldConfig
  application:
    name: config-server